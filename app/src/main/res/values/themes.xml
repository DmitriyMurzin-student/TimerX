<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CoolTimerX" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/my_light_primary</item>
        <item name="colorAccent">@color/speek</item>
        <!-- Установите цвет для ActionBar (верхнего меню) -->
        <item name="android:colorPrimary">@color/my_light_primary</item>
        <item name="android:colorAccent">@color/speek</item>
        <item name="android:preferenceStyle">@color/speek</item>
    </style>

    <style name="Theme.CoolTimerX" parent="Base.Theme.CoolTimerX" />
</resources>